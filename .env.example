# Copy to .env and set a strong value. The fixed bearer key clients must present.
# This file is an EXAMPLE — never put a real secret here, never commit .env.
API_KEY=replace-with-a-long-random-secret
