# tests/

Evidence. Tests must be meaningful, not tautological (see `../AGENTS.md` §8).

Planned:
- `test_repo_sanity.py` — verifies the governance scaffold exists (present now).
- real-detection test with a committed fixture image (WO-01).
- schema, negative-path, and real-`openai`-client compatibility tests (WO-02/03).

`fixtures/` holds small committed test images.
