# Runtime dependencies (lean: no torch, no ultralytics).
# Conservative floors only — WO-01 must produce a committed lockfile with
# EXACT pinned versions. Mirror of [project.dependencies] in pyproject.toml.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
onnxruntime>=1.18,<2.0
pillow>=10.3,<12.0
numpy>=1.26,<3.0
